extends: default

rules:
  line-length:
    max: 100
    ignore: |
      /charts*/
  document-start: disable
  indentation:
    # Sequences are usually not indented in the Kubernetes world
    indent-sequences: true

ignore: |
  /.github/
  /venv*/
